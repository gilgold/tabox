node_modules
build
build-firefox/
*.log
.yarn/*
!.yarn/patches
!.yarn/releases
!.yarn/plugins
!.yarn/sdks
!.yarn/versions
.pnp.*
.codex/
.claude/
.cursor/
api-keys.json
chrome/api-keys.json
chrome/api-keys.local.json
.vscode
.DS_Store
*.zip
coverage
test-results
playwright-report
.superpowers/

# Local competitive/market research notes (not for publishing)
market-research/

# Stale build output / scratch artifacts (not part of the product)
v4/
output/
# Local seed/test data scripts
seed-test-data.js
seed-test-data-standalone.js
quick-seed.js
# Cross-project working notes (crxbox), kept locally, not committed to tabox
crxbox-assessment.md
crxbox-feedback.md

# Local design docs / plans — not tracked
docs/

# Marketing assets / copy — not tracked
marketing/

# Pricing page: local env + generated output (token lives here; don't commit)
site/pricing/.env
site/pricing/pricing.html

# Local env files (API tokens) — never commit
.env.local
.env*.local
server/.wrangler/
